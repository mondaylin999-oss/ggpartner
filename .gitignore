# Netlify's local link (carries the site id for this machine)
.netlify

# Build output — regenerated by ./build.sh, never edited by hand
dist/

# Local copies of the app from before the pixel restyle and the file split.
# Kept on disk as a rollback path; not part of the source.
backups/

# macOS
.DS_Store
